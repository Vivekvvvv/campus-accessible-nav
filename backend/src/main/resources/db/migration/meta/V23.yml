strategy: expand
backward_compatible: true
owner: backend-platform
rollout: add tenant_id to core tables with safe defaults before enabling tenant-aware reads and writes
rollback: keep defaulted tenant_id columns and revert application tenant isolation paths
compat_window: two releases
